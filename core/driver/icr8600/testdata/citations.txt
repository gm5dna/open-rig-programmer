# IC-R8600 — the ALLOWED CITATIONS of core/driver/icr8600 and core/civ/icr8600.
#
# This list is the POSITIVE half of the provenance pins. The blacklist beside
# it (core/driver/icr8600/provenance_test.go) names literals a rejected branch
# actually copied from another radio; a paraphrase walks past it. This list
# asks the other question, and it is the one the tier review asked for: of
# every document citation these two packages make, can this model's OWN
# authority supply it? A citation not written here fails the test, and the
# answer is to check the authority — never to widen the list to fit the prose.
#
# The authority. core/driver/icr8600/doc.go states this package's scope but
# never names the document, so the naming below is copied verbatim from §0 of
# docs/superpowers/icom-matrices/icr8600-capability-matrix.md, which is the
# reading the package SHA-pins in core/civ/icr8600/crosscheck_test.go:
#
#   Title (metadata) "IC-R8600 CI-V REFERENCE GUIDE"; Author "Icom Inc."
#   "Revision 3a; printed document code A7375-2EX-3a (back cover, PDF p.28)"
#   "28 PDF pages, printed folios 1-26 plus cover and back cover, A4"
#
# The reading is that matrix, its report and its review, together with
# docs/superpowers/plans/2026-08-28-icom-icr8600.md.
#
# FORMAT. One canonical citation token per line; "#" starts a comment, and a
# token may carry a trailing note. The tokens are produced by
# core/driver/internal/drivertest.ScanCitations, whose grammar and the two
# shapes it deliberately omits are documented in citations.go.
#
# WHERE THE AUTHORITY IS ABSENT. docs/superpowers/ is gitignored, so a fresh
# clone and CI do not have it (the v1.2.1 CI run, 30/08/2026). This list is
# checked in and is ALWAYS enforced; the supply check against the authority
# runs only where the authority is present, and logs a skip where it is not.


# Pages of the authority PDF.
PDF p.12
PDF p.13
PDF p.15

# Page spans.
PDF pp.13-15

# Matrix sections.
matrix §1
matrix §1b.2
matrix §1b.3
matrix §2
matrix §3.14
matrix §3.15.7

# Assumption-register entries.
icr8600-address-move
icr8600-answer-length
icr8600-antenna  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
icr8600-attenuator  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
icr8600-baud-set
icr8600-budget
icr8600-control-lines
icr8600-default-baud
icr8600-digital-tail-template  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
icr8600-dtcs-chart
icr8600-echo-default
icr8600-empty-reply-fa
icr8600-empty-reply-ff
icr8600-filter-byte-digital
icr8600-id-token
icr8600-ip-plus  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
icr8600-mode-wire-codes
icr8600-name-charset-codes
icr8600-name-pad
icr8600-preamp  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
icr8600-program-tuning-step  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
icr8600-progstep-floor
icr8600-read-request-form
icr8600-record-lengths
icr8600-scan-edge-encoding
icr8600-serial-framing
icr8600-short-set
icr8600-tail-templates
icr8600-transceive-default
icr8600-ts-off-code
icr8600-tsql-chart-bounds
icr8600-tuning-range
icr8600-tuning-step  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
icr8600-tuning-step-enabled  # named by the implementation plan, not by the matrix, which grades this D8 field in §1b.2 without giving it an id
