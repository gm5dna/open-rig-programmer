# M9d-1 FTdx101 dialect — Implementation Plan (revision 1)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `core/cat/ftdx101` — the FTdx101D/MP's shared dialect (TWO
exported instances differing only in CAT ID) and chart-transcribed EX
inventory, proven by conformance, identity/difference pins,
assumption-itemised goldens, an independent frame-geometry witness and
a model-applicability sweep.

**Spec:** `docs/superpowers/specs/2026-08-05-m9d-ftdx101-design.md`
(rev 2) — the authority on every evidence rule. **Only the orchestrator
and the Task 3/5/6/7b/8 implementers read it (or this plan).**

**Architecture:** exact sibling of `core/cat/ftdx10` (M9c-4's
deliverable, plan `2026-07-29-m9c4-ftdx10-dialect.md`, executed
revision 2) — every mechanism reused, nothing invented; the deltas are
the two-instance construction, the W witness and the L applicability
sweep, all mandated by spec rev 2.

**Tech stack:** Go, `cat.MustNewDialect`, `internal/extable` profile
registry, `core/cat/dialecttest`.

## THE QUARANTINE (the central mechanism — read first)

Tasks 1 (ledger), 2 (geometry witness), 4 (transcription B) and 7a
(golden vectors) are executed by fresh-context agents — **L**, **W**,
**B** and **G** — from SELF-CONTAINED briefs the orchestrator writes.
The quarantine is MATERIAL, not instructional:

- **L, W, B and G never open the repository.** Each brief contains:
  the PDF's ABSOLUTE path (`docs/fixtures-private/manuals/
  ftdx101_cat_2308-L.pdf` under the repo root — the one permitted
  in-repo read), an output path in the session SCRATCHPAD, the exact
  output format, the verbatim-transcription rules, and the STOP
  conditions. Nothing else.
- **Forbidden to L, W, B and G, stated in each brief:** this plan, the
  spec, the adjudications, `ftdx101_layout.txt`, ANY ftdx10 file, any
  other repository file, any row count, any frame byte-count, any
  address, any prior task's output, and any orchestration history.
- **W works from RASTERISED pages only** (`pdftoppm`-rendered, cropped
  charts — the M9c-4 goldens' method, `core/cat/ftdx10/testdata/
  provenance.md`): its brief contains NO claimed widths, offsets or
  totals, and no statement of the grid's header convention — the
  rendered image is the evidence.
- **The orchestrator moves each scratchpad output into the tree,
  reviews it for format only (never "corrects" content), and commits
  it.** Hashes recorded at commit time; any later change to a
  quarantined artefact is a STOP.
- L, W, B and G are DISJOINT agents, and disjoint from every other
  task's implementer.

## Global Constraints

- British English; SPDX (`GPL-3.0-or-later`) on every new `.go` file;
  `gofmt -l .` silent per task; never regenerate any golden.
- Per-task mechanical gates, EVERY task from its creation onward:
  `git diff --exit-code -- core/cat/testdata/ core/cat/exinventory_gen.go
  core/cat/ftdx10/testdata/ core/cat/ftdx10/exinventory_gen.go
  internal/fakedx10/exinventory_gen.go internal/fakedx10/transcription-b.csv`
  AND (once they exist)
  `git diff --exit-code -- core/cat/ftdx101/testdata/ core/cat/ftdx101/exinventory_gen.go`
  — the new artefacts get the never-regenerate discipline from birth
  (the golden gate is EIGHT paths when M9d-1 closes; ten arrives with
  M9d-2's fakedx101 pair).
- STOPs are real and orchestrator-arbitrated: PDF ambiguity (Tasks
  1/2), any model-conditional marking touching a STORED property
  (Task 1's sweep), a numeric Digits > 4 (Tasks 3/4), the Task 3
  preflight mismatch, any cross-check mismatch (Task 5), any
  W-vs-dialect geometry disagreement (Task 6 gate), any
  golden-vs-codec mismatch (7b). **Arbitration may correct A, B, the
  ledger, OR the witness** — never by consensus, always against the
  PDF, and A is never edited merely to satisfy `ExpectedRows`.
- Tasks execute SEQUENTIALLY on branch `m9d1-ftdx101-dialect` (one
  shared worktree; no parallel commits).
- Fresh Opus implementer per non-quarantined task; orchestrator
  verifies every landing (standing model).

## File Structure

| File | Task | Responsibility |
|---|---|---|
| `core/cat/ftdx101/testdata/group-ledger.csv` (+ `.md` companion) | 1 | PDF-derived ledger + the model-applicability attestation |
| `core/cat/ftdx101/testdata/geometry-witness.csv` (+ `.md`) | 2 | raster-counted MR/MT/MW Set+Answer positions |
| `core/cat/ftdx101/table2.csv` | 3 | transcription A + provenance header |
| `internal/extable/profile.go` (+ `profile_test.go`) | 3 | the `"ftdx101"` registration + membership-pin update |
| `core/cat/ftdx101/exinventory.go`, `doc.go`, `exinventory_gen.go`, `staleness_test.go` | 3 | directive; doc + ASSUMED register + chart-defect record; generated; package-local staleness |
| `core/cat/ftdx101/testdata/transcription-b.csv` | 4 | the semantic tuple, PDF-primary |
| `core/cat/ftdx101/crosscheck_test.go` | 5 | A ≡ B ≡ ledger, full legs |
| `core/cat/ftdx101/dialect.go`, `dialect_test.go` | 6 | TWO instances; conformance ×2; identity/difference/sibling pins; EX answer bound |
| `core/cat/ftdx101/testdata/*.golden` | 7a | vectors (quarantined G) |
| `core/cat/ftdx101/golden_test.go` | 7b | mechanical byte-compares + the D-vs-MP frame-identity test |
| `docs/superpowers/m9d1-baseline-note.md` | 8 | the FULL byte-identity re-run vs the M9c-6 manifest |

---

### Task 1: the group-boundary ledger + applicability sweep (quarantined L)

- [ ] Orchestrator writes L's self-contained brief per THE QUARANTINE:
  PDF absolute path; scratchpad output path; the EXACT format —
  **`group-ledger.csv`** with header
  `p1,p2,p1_label,p2_label,first_p3,first_name,last_p3,last_name,row_count,pdf_page,visual_anchor`
  (one row per (P1,P2) subgroup of the Table 2 "MENU Chart",
  labels/names VERBATIM including punctuation, from the rendered PDF's
  ruled cells only) plus a `.md` prose companion (method, date, manual
  revision, pages, nothing-else-consulted statement, STOP findings).
- [ ] **The applicability sweep (spec rev 2, adjudication A3), in the
  same brief:** for EVERY Table 2 row, record in the `.md` companion
  any marking that conditions the row's EXISTENCE, its P1/P2 labels,
  its function name, its Digits, or its textness on the radio model
  (the manual covers two models); parameter-VALUE ranges that differ
  by model are recorded verbatim but are NOT such a marking. Close
  with one of two verbatim attestations: "NO row's stored properties
  are model-conditional" or an enumerated list. Ambiguity = STOP
  finding, never a choice.
- [ ] Orchestrator: move both files into `core/cat/ftdx101/testdata/`,
  format-review only, commit
  (`M9d-1 task 1: the PDF-derived group-boundary ledger and the
  model-applicability attestation`), record SHA-256s. THEN
  (orchestrator, not L): sum `row_count` — the expected total is 193
  (two prior derivations on record: the roadmap estimate and the spec
  reviewer's independent count); anything else, or a non-empty
  conditional-property list, is a STOP-and-arbitrate against the PDF
  before Task 3 launches.

### Task 2: the frame-geometry witness (quarantined W)

- [ ] Orchestrator writes W's self-contained brief per THE QUARANTINE:
  PDF absolute path; scratchpad output paths; the method — render the
  pages containing the MR, MT and MW command sections with `pdftoppm
  -r 200 -png`, crop each command's Set and Answer position grids, and
  COUNT from the images; the EXACT format —
  **`geometry-witness.csv`** with header
  `command,direction,token,first_pos,last_pos` (one row per parameter
  token INCLUDING the literal command letters and the terminating
  `;`, positions 1-based; `direction` in {set,read,answer}) plus a
  `.md` companion (rendered pages, crop method, the position
  arithmetic written out per grid, anomalies). The brief contains NO
  expected totals, widths, offsets, or grid-convention statements.
  Any grid whose position row cannot be read unambiguously from the
  raster = STOP finding.
- [ ] Orchestrator: move both files into `core/cat/ftdx101/testdata/`,
  format-review only, commit (`M9d-1 task 2: the raster-counted
  frame-geometry witness`), record SHA-256s. The orchestrator does
  NOT compare against expected values yet — the comparison is Task
  6's acceptance gate, so a wrong hypothesis is caught against the
  dialect, not silently "fixed" in the witness.

### Task 3: transcription A + profile + generation (implementer A2 — reads plan+spec, NOT the ledger, NOT the witness)

- [ ] Transcribe layout-text-led, PDF-checked, into
  `core/cat/ftdx101/table2.csv` (the extable 10-column shape; names
  VERBATIM including punctuation; wrapped rows joined — the chart
  wraps at least one name across two printed lines; provenance header
  recording: manual rev 2308-L, the EX grammar header-vs-chart
  anomaly IF the chart's populated P1 range differs from the
  header's, the model-conditional P4 value ranges at the MAX POWER
  rows transcribed verbatim with both models' figures, and any
  printed P4 self-inconsistency found there — record, don't
  resolve). **A2 does not open the ledger or the witness** — the
  orchestrator's brief hands A2 the single number
  `ExpectedRows: 193` extracted from the ledger.
- [ ] **The preflight, explicit and BEFORE generation:** count A's
  data rows (`grep -vc '^#' core/cat/ftdx101/table2.csv`) and compare
  to 193. Mismatch = STOP — report, do not touch the CSV. Only then:
  register the `"ftdx101"` profile in `internal/extable/profile.go`:

```go
// ftdx101Profile carries the FTdx101D/MP's Table 2 transcription facts —
// ONE profile for BOTH models, because the spec-reviewed applicability
// sweep (core/cat/ftdx101/testdata/group-ledger.md) attests that no
// stored property is model-conditional; only P4 VALUE ranges differ, and
// P4 semantics are not stored. Same TypesImported shape as ftdx10.
var ftdx101Profile = Profile{
	Model:       "FTdx101D/MP",
	Package:     "ftdx101",
	Types:       TypesImported,
	ImportPath:  "github.com/gm5dna/open-rig-programmer/core/cat",
	ImportAlias: "cat",
	VarName:     "exItems",
	OutFile:     "exinventory_gen.go",
	ManualCSV:   "table2.csv",

	MinDigits: 1,
	MaxDigits: 4,
	TextWidth: 12,
	// MaxObservedWidth is an INERT API-REQUIRED SENTINEL here, exactly as
	// on the ftdx10 profile: ObservationsAbsent means no observation CSV
	// is ever parsed and this bound is never consulted. No hardware claim.
	MaxObservedWidth: 12,
	// ExpectedRows comes from the group-boundary ledger
	// (core/cat/ftdx101/testdata/group-ledger.csv), derived from the
	// rendered PDF before any transcription existed. If A and B agree on
	// a number that is not this one, the answer is arbitration against
	// the PDF, never an edit here.
	ExpectedRows: 193,

	Observations: ObservationsAbsent,
	DocLines: []string{
		"exItems is the FTdx101D/MP's EX address inventory, sorted by (P1,P2,P3),",
		"built from ONE source: the manual transcription in table2.csv (the",
		`FTDX101MP/FTDX101D CAT manual rev 2308-L's Table 2 "MENU Chart"). It`,
		"serves BOTH models: the ledger's applicability attestation records that",
		"no stored property is model-conditional. There are no hardware READ",
		"observations to join — no FTdx101 has ever been asked anything — so",
		"every item carries the absence sentinels ObservedReadWidth 0 and",
		"ObservedReadShape \"\". Regenerate with `go generate ./core/cat/ftdx101`;",
		"do not edit by hand.",
	},
}
```

  and add `"ftdx101": ftdx101Profile` to the `mustRegistry` map;
  update `profile_test.go`'s registry membership pin (the exact-list
  test at ~:349-374) from `{ft710, ftdx10}` to
  `{ft710, ftdx10, ftdx101}` — this pin is INSIDE the spec's stated
  change boundary.
- [ ] Write `core/cat/ftdx101/exinventory.go` (the
  `//go:generate go run github.com/gm5dna/open-rig-programmer/internal/extable/gen -profile ftdx101`
  directive, mirroring `core/cat/ftdx10/exinventory.go`) and `doc.go`
  (provenance; the dialect-level ASSUMED register skeleton — entries
  land in Task 6; the chart-defect record: the EX grammar
  header-vs-chart anomaly as found, the MAX POWER P4 conditionals,
  any printed inconsistency); run `go generate ./core/cat/ftdx101`;
  commit the generated file. A RenderGo row-count refusal at this
  point IS the preflight STOP having been missed — do not add or
  remove rows to satisfy it.
- [ ] `staleness_test.go`: registry-selected
  (`extable.RegisteredProfiles()` filtered to `Package == "ftdx101"`,
  exactly one), re-derive, byte-compare — mirror
  `core/cat/ftdx10/staleness_test.go`.
- [ ] Tests: `go test ./internal/extable/ ./core/cat/ftdx101/ -count=1`;
  gofmt; both golden diff gates. Commit: `M9d-1 task 3: transcription
  A, the ftdx101 extable profile, and the generated inventory`.

### Task 4: transcription B (quarantined B)

- [ ] Orchestrator writes B's self-contained brief per THE QUARANTINE:
  PDF absolute path; scratchpad output path; the CSV shape
  `p1,p2,p3,p1_label,p2_label,name,digits,text` (semantic tuple, no
  P4, no line refs); verbatim-names rule (punctuation included; a
  name printed across two lines is ONE name); work from the rendered
  PDF's ruled cells ONLY; a numeric digits value above 4 = STOP
  finding recorded in the report AND transcribed as seen. **No
  counts, no addresses, no other files.**
- [ ] Orchestrator: move to
  `core/cat/ftdx101/testdata/transcription-b.csv`, format-review
  only, commit (`M9d-1 task 4: transcription B — the PDF-primary
  semantic tuple`), record SHA-256.

### Task 5: the cross-check (implementer; reads plan+spec)

- [ ] `crosscheck_test.go`: parse A via
  `extable.ParseCSV(profile, ...)`; parse B and the LEDGER CSV with
  `encoding/csv` (no regexp over prose). Bind ALL legs, mirroring
  `core/cat/ftdx10/crosscheck_test.go`:
  - A ≡ B per address on the FULL tuple (labels, name, digits, text —
    exact strings); address sets equal BOTH directions.
  - For A and for B against the ledger: (P1,P2) group-key sets equal
    BOTH directions; per-group `row_count` equal; per-group labels
    equal; per-group first/last `(P3, name)` equal.
  - Totals: A == B == ledger sum == the profile's `ExpectedRows`.
  - **The text-row pin, fully hardcoded:** exactly one `text=true`
    row in EACH of A and B, at address (04,01,01), `p1_label`
    "DISPLAY SETTING", `p2_label` "DISPLAY", `name` "MY CALL."
    (verbatim, trailing full stop), `digits` 12 == the profile's
    TextWidth. A mismatch here is a STOP, not a test edit: the pin's
    values are the orchestrator's chart hypothesis and arbitration is
    against the PDF.
- [ ] ANY mismatch = STOP: report exact rows; the orchestrator
  arbitrates against the PDF and directs the fix to **A, B, or the
  ledger**, never both/all silently; a ledger fix re-runs Task 1's
  total-and-attestation check.
- [ ] Tests: `go test ./core/cat/ftdx101/ -count=1`; gofmt; both
  golden diff gates. Commit: `M9d-1 task 5: the A/B/ledger
  cross-check binds every leg`.

### Task 6: the two dialect instances + pins (implementer; reads plan+spec)

- [ ] `dialect.go` — ONE package-private constructor, TWO instances;
  every field explicit including the required zeros; modeNames
  transcribed FRESH from THIS manual (the legends printed beside MD,
  MR, MT and MW; the OI legend carries a PRINTING ERROR — a
  duplicated "E:" prefix on its last two members — which is recorded
  in doc.go's chart-defect record and excluded from sourcing, not
  silently resolved):

```go
// newDialect builds the FTdx101 family dialect for one CAT ID. D and MP
// share EVERY protocol byte but the ID answer (the ledger's
// applicability attestation is the evidence); the two instances exist so
// that probe/identity can be honest per model.
func newDialect(catID string) cat.Dialect {
	return cat.MustNewDialect(cat.DialectConfig{
		CATID:     catID,
		ModeNames: modeNames, // fresh transcription, 1..F per this manual;
		// '0' (cat.ModeUnset, "-") included as an ASSUMED member — absent
		// from every FTdx101 legend; parsers must accept the placeholder.
		Slots: cat.SlotSpace{
			MemoryLo: 1, MemoryHi: 99,
			// ASSUMED bounds — the FTdx101 legends say only "5xx (5MHz
			// BAND)"; 501..599 is interpretation inherited from the
			// FT-710/FTdx10, both unverified. Register entry.
			SixtyLo: 501, SixtyHi: 599,
			PMSPairs:      9,
			EmergencyWire: "EMG",
			NoneWire:      "000", // ASSUMED — in no FTdx101 slot legend
		},
		EXItems: exItems,
		MT: cat.MTPolicy{
			Form:         cat.MTFormCombined,
			TagMaxBytes:  12,
			ClearTagByte: 0,   // must be 0 under MTFormCombined (V9)
			PadByte:      0,   // must be 0 under MTFormCombined (V9)
			TagFill:      ' ', // ASSUMED — never observed on this radio
		},
		Clarifier: cat.ClarifierPolicy{
			StepHz:   10, // ASSUMED — no step stated in the manual
			MaxAbsHz: 9990,
		},
		MWWriteKind: cat.CombinedMTSetKind, // MW P7 "(Fixed)" — a fact
		// of this radio, not a rule; see the difference pins.
	})
}

var (
	dialectD  = newDialect("0681")
	dialectMP = newDialect("0682")
)

// DialectD is the FTDX101D (CAT ID 0681).
func DialectD() cat.Dialect { return dialectD }

// DialectMP is the FTDX101MP (CAT ID 0682).
func DialectMP() cat.Dialect { return dialectMP }
```

- [ ] `dialect_test.go`:
  - `dialecttest.Run(t, DialectD())` AND `dialecttest.Run(t,
    DialectMP())`; `dialecttest.RunZeroValue` once.
  - Mode identity (256-sweep `ValidMode`/`ModeName` vs `cat.FT710`;
    `ModeByName` round-trips; ASSUMED members noted).
  - Slot identity (behavioural: `ParseSlot` over 000-999 + P1L..P9U +
    "EMG", acceptance and `Wire()` equality vs `cat.FT710`).
  - **Sibling pins (D vs MP):** `DialectD().CATID() == "0681"`,
    `DialectMP().CATID() == "0682"`; mode tables equal over all 256
    bytes; slot behaviour equal over the full sweep;
    `EXAddresses()` equal element-wise; `MTForm`, `MTAnswerBounds`,
    `MWWriteKind`, `Clarifier` accessors equal — the enumerated
    claim "they differ ONLY in CAT ID" asserted accessor by
    accessor.
  - Difference pins vs the OTHER radios: CAT IDs pairwise distinct
    ({0681, 0682} vs FT-710's and the FTdx10's); vs `cat.FT710`:
    `MTForm() == cat.MTFormCombined` (≠ short),
    `MWWriteKind() == cat.CombinedMTSetKind` (≠ the FT-710's);
    `MTAnswerBounds()` = (41, 41, nil); vs `ftdx10.Dialect()`: at
    least two EX addresses present in exactly ONE of the two
    inventories, chosen from the two committed `table2.csv` files
    with citation comments, asserted BOTH directions.
  - The EX answer bound (independent `max(Digits)` from `EXItems()`
    == 12; a 12-byte P4 answer accepted via `ParseEXAnswer`; 13
    rejected).
- [ ] `doc.go`: the dialect-level ASSUMED register lands in full
  (M9c-4's six-entry shape: TagFill ' '; the exact 41-byte combined
  answer width; NoneWire "000"; ModeUnset '0'; 5xx numbering
  501..599; clarifier StepHz 10 — each with its named Stage R lifting
  capture, PER MODEL: a D capture does not lift an MP entry).
- [ ] **The geometry gate (orchestrator, at landing review — spec rev
  2, adjudication A2):** compare `testdata/geometry-witness.csv`
  against the dialect's geometry — the MT Set and Answer `;` at 41
  with the tag tokens at 29..40, MR answer `;` at 28, MW Set `;` at
  28, the MR/MT Read forms `;` at 6 — token by token. ANY
  disagreement = STOP-and-arbitrate against
  the PDF (the witness, the dialect, or the spec hypothesis is
  wrong; the fix is directed, never consensus).
- [ ] Tests: `go test ./core/cat/ftdx101/ ./core/cat/... -count=1`;
  gofmt; both golden diff gates. Commit: `M9d-1 task 6: the two
  FTdx101 dialect instances, conformance, and the
  identity/difference/sibling pins`.

### Task 7a: golden vectors (quarantined G) · 7b: the tests

- [ ] **7a** — orchestrator writes G's self-contained brief per THE
  QUARANTINE: PDF absolute path; the Answer-chart guidance (the MT
  ANSWER position chart is the clean one; the Set chart interleaves
  with its legend; work only within the command section whose
  two-letter code heads it — adjacent sections' titles resemble each
  other); the vector classes (≥3 combined-MT Answer vectors:
  full-width tag, short tag padded — padding bytes marked
  INHERITED-ASSUMED — and a cleared tag; MR and MW vectors; 3 EX
  read vectors); the per-class provenance-itemisation requirements
  from the spec (manual rev/page/direction; session/date;
  no-code-consulted; manual-documented vs inherited-assumed bytes;
  UNVERIFIED status; the Stage R capture that lifts each).
  Scratchpad out; orchestrator moves to
  `core/cat/ftdx101/testdata/*.golden`, commits (`M9d-1 task 7a:
  assumption-itemised manual-derived golden vectors`), **records
  each file's SHA-256 in the commit message**.
- [ ] **7b** — a separate, code-aware implementer writes
  `golden_test.go`: parse/build each vector through `DialectD()`;
  gate-admissibility for Set-direction vectors; byte equality; PLUS
  **the D-vs-MP frame-identity test** — every Set-direction golden
  built through BOTH instances byte-compares equal, and a
  representative frame set (MT Set, MW Set, MT read, EX read)
  built/parsed through both instances agrees byte-for-byte, so the
  sibling claim is proven at the FRAME level, not only the accessor
  level. **It may not modify any vector** — the per-task diff gate
  over `ftdx101/testdata/` enforces this mechanically from 7a's
  commit onward. A golden-vs-codec mismatch = STOP.
  Tests: `go test ./core/cat/ftdx101/ -count=1`; gofmt; ALL golden
  diffs. Commit: `M9d-1 task 7b: the golden byte-compare tests and
  the D-vs-MP frame identity`.

### Task 8: full gate + the FULL byte-identity re-run

- [ ] Full local gate: gofmt; `go build ./...`; `go vet ./...`; full
  `go test ./... -count=1` foreground (`-race` over `./core/...` in
  the background); `go test ./internal/guards/ -v` (all guards by
  name); every golden diff gate (all EIGHT paths).
- [ ] **The COMPLETE M9c-6 manifest recipe**: every artefact the
  M9c-6 manifest records (FT-710 AND FTdx10 legs — probe / read /
  read-settings / diff / CHIRP-import / CSV-export ×
  stdout/stderr/exit/files, with the recorded normalisations),
  compared against the M9c-6 manifest's hashes. Record in
  `docs/superpowers/m9d1-baseline-note.md`. **Expected: ZERO diffs —
  spec rev 2's M9d-1 claim, independently judged feasible by both
  spec reviewers.** Any difference is a STOP.
- [ ] Commit the note. Milestone then goes to the standing parallel
  Codex + Fable milestone review before merge.

## Self-review

Coverage: ledger+sweep→T1; witness→T2; A+profile+generation+staleness→T3;
B→T4; cross-check→T5; two instances+pins+register+geometry gate→T6;
vectors→7a; golden tests+frame identity→7b; gate+byte-identity→T8.
Spec rev 2 items: geometry witness (T2+T6 gate); applicability sweep
(T1); record-don't-resolve quirks (T3 provenance header, T6 OI
exclusion, briefs); two instances (T6); D-vs-MP frame identity (7b);
membership pin inside boundary (T3); zero-diff acceptance (T8).
Quarantine: L/W/B/G never open the repo; A2 never opens the ledger or
witness; W's brief carries no widths and no grid-convention statement;
briefs carry no counts. Ordering: strictly sequential; T1 before T3
(the extracted total); T2 any time before T6's gate; T5 after 3+4; T6
after 5; 7a any time after T1; 7b after T6; T8 last. Placeholders:
none — the text-row pin is hardcoded, the profile literal is complete,
the witness format is pinned, every gate is an enumerated command.
